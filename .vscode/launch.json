{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Django",
            "type": "debugpy",
            // changed to debugpy because python is deprecating soon.
                //TODO:  search this up later
            "request": "launch",
            "program": "${workspaceFolder}/manage.py",
            "args": ["runserver"],
            "django": true,
            "justMyCode": true,
            "cwd": "${workspaceFolder}"
        }
    ]
}
